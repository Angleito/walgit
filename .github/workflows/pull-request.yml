name: Pull Request Checks

on:
  pull_request:
    branches: [ main ]

jobs:
  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: WalGit-Backend/package-lock.json
    
    - name: Install dependencies
      run: |
        cd WalGit-Backend
        npm ci
    
    - name: Run tests
      run: |
        cd WalGit-Backend
        npm test

  frontend-tests:
    name: Frontend Tests
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: WalGit-frontend/package-lock.json
    
    - name: Install dependencies
      run: |
        cd WalGit-frontend
        npm ci
    
    - name: Run tests
      run: |
        cd WalGit-frontend
        npm test
    
    - name: Run lint
      run: |
        cd WalGit-frontend
        npm run lint

  smart-contracts:
    name: Smart Contract Tests
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Rust
      uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: stable
        override: true
    
    - name: Install Sui CLI
      run: |
        cargo install --locked --git https://github.com/MystenLabs/sui.git --branch main sui
    
    - name: Build and test Move contracts
      run: |
        cd move
        sui move build
        sui move test